# URL do backend WebSocket
VITE_WS_URL=ws://localhost:8000

# Path base do app no servidor (ex: / ou /planningpoker/)
VITE_APP_BASE=/

# Produção — copie para .env.production e ajuste com sua URL do Render
# VITE_WS_URL=wss://seu-app.onrender.com
